# Styles

Global design tokens and component styling currently live in
`app/globals.css`, following the Next.js App Router convention. Add isolated
style modules here when a future feature needs styles outside the global design
system.
